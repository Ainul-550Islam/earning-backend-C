[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 300"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
