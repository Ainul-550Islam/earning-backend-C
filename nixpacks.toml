[phases.setup]
nixPkgs = ["python311"]
apt = ["libpq-dev", "gcc", "libcairo2-dev", "pkg-config", "python3-dev"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python3.11 -m gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
