[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.deploy]
cmds = [
  "python manage.py migrate --noinput",
  "python manage.py seed_tenant"
]

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
