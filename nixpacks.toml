[phases.setup]
nixPkgs = ["python311", "libpq"]
apt = ["libpq-dev", "gcc", "libcairo2-dev", "pkg-config", "python3-dev"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
