[phases.setup]
nixPkgs = ["python311", "postgresql"]
apt = ["libpq-dev", "gcc", "libcairo2-dev", "pkg-config", "python3-dev"]

[phases.install]
cmds = ["pip3 install -r requirements.txt"]

[start]
cmd = "python3 -m gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
