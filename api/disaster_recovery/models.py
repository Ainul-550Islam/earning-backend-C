"""
Django models stub for the Disaster Recovery app.

The DR system uses SQLAlchemy (not Django ORM) for its own database.
All SQLAlchemy model definitions live in `sa_models.py`.
This file exists only so Django's app registry doesn't crash when it
tries to import `<app>.models` for every entry in INSTALLED_APPS.
"""
# No Django models — intentionally empty.
# See sa_models.py for the SQLAlchemy table definitions.
